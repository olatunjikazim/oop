console.log("codevillage OOP class")


let baseSalary = 30 _000;
let incentive = 10
let points = 30

function getMonthlyPay(baseSalary, incentive, points) {
    return baseSalary + incentive + points;
}


let employee = {
    baseSalary: 40 _000,
    incentive = 1000,
    points = 290,
    getMonthlyPay: function() {
        return this.baseSalary + this.incentive + this.points
    }



        const rectangle = {

        width: 10,
        height: 10,
        location: { x: 0, y: 0 },
        draw() {
            console.log(`drawing...`

                spec)

        }
    }


}

const createName = (firstName, lastName) => ({

})

function rectangle(width, height) {
    this.width: width;
    this.height:
}